modules = ["nodejs-20"]

[nix]
channel = "stable-24_05"

[workflows]
runButton = "Project"

[[workflows.workflow]]
name = "Project"
mode = "parallel"
author = "agent"

[[workflows.workflow.tasks]]
task = "workflow.run"
args = "Angular Todo App"

[[workflows.workflow]]
name = "Angular Todo App"
author = "agent"

[[workflows.workflow.tasks]]
task = "shell.exec"
args = "cd src && npm install --legacy-peer-deps && ng serve --host 0.0.0.0 --port 5000"
waitForPort = 5000

[deployment]
run = ["sh", "-c", "cd src && npm install --legacy-peer-deps && ng serve --host 0.0.0.0 --port 5000"]
